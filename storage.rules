rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // gallery 폴더 및 하위 모든 파일에 대한 규칙
    match /gallery/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
